{
  "abDS": {
    "host": "${MONGO_DB_HOST}",
    "port": "${MONGO_DB_PORT}",
    "url": "${MONGO_DB_URL}",
    "database": "${MONGO_DB_DATABASE}",
    "password": "${MONGO_DB_PASSWORD}",
    "name": "${MONGO_DB_NAME}",
    "user": "${MONGO_DB_USER}",
    "connector": "mongodb"
  }
}
